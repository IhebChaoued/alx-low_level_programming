#include <stdio.h>
/**
 * main - Entry point.
 *
 * Return: 0 indicate success.
 */
int main(void)
{
	int a = 97;

	for (int i = a + 25; i >= a; i--)
	{
		putchar('i');
	}

	putchar('\n')

	return (0);
}
